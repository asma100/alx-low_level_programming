#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
char alaph;
for (alaph = 'z'; alaph >= 'a'; alaph--)
putchar(alaph);
putchar('\n');
return (0);
}
