/**
 * rev_string - reverses a string
 * @s: string to be reversed
 */
void print_rev(char *s)
{
while (*s != 0)
{

	int l = 0;
	int m;

	while (*s != '\0')
	{
		l++;
		s++;
	}
	s--;
	for (m = l; m > 0; m--)
	{
		_putchar(*s);
		s--;
	}
	_putchar('\n');
}
}

