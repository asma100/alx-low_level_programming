#include "main.h"
/**
* print_square - square
* @size: square size
* Return: 0
*/
void print_square(int size){
int n, m;
if (!(size < 0))
{
for (m = 0; m < size ;m++)
{
for (n = 1; n <= size; n++)
{
_putchar('#');
}
_putchar('\n');
}
}
else 
_putchar('\n');
}
