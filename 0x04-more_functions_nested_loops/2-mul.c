#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: is the int to be multiplies
 * @b:is the int to be multiplies
 * Return: x the result of  multiplication.
 */
int mul(int a, int b)
{
int long x;
x = a * b;
return (x);
}
